# AR Target

The compiled MindAR image target is stored here:

```text
public/targets/meimeisho.mind
```

`meimeisho.png` is the Canva naming card image used as the source target. The app checks `meimeisho.mind` before starting AR. If it is missing, `/ar` falls back to the normal album link.
