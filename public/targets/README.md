# AR Target

Place the compiled MindAR image target here:

```text
public/targets/meimeisho.mind
```

Use the exported naming card image as the source target. The app checks this file before starting AR. If it is missing, `/ar` falls back to the normal album link.
