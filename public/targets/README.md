# AR Target

The compiled MindAR image target is stored here:

```text
public/targets/birth-card.mind
```

`birth-card.png` is the Canva birth card image used as the source target. The app checks `birth-card.mind` before starting AR. If it is missing, `/ar` falls back to the normal album link.
